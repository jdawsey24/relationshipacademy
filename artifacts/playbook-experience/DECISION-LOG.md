# Playbook Experience — Decision Log (Cluster 1: Difficulty Feeling Chosen)

Chronological record of approvals + rulings for the Playbook *experience* phase (design + UX architecture).
Upstream behavioral architecture lives in `../behavioral-derivation/`.

| # | Type | Item | Decision |
|---|---|---|---|
| 1 | Approval | Experience Design Spec v1 | Approved at the structural level. |
| 2 | Correction | Design Spec · B2 Reciprocity-Based Investment | Removed "steadiness can feel less exciting at first" (unsupported psychological assumption). Discomfort reframed: reducing pursuit/over-functioning creates uncertainty because the user leaves more relational space and lets the other person's behavior become observable. Removed the "match effort for a couple of weeks" micro-practice (no tit-for-tat, no arbitrary timeline). New logic: keep showing interest naturally while leaving space to observe initiation/follow-through/investment — reciprocity is information, not scorekeeping. |
| 3 | Correction | Design Spec · Intentional Dating Break | Duration is OPTIONAL (no fixed calendar). Primary logic = intentionality: what I'm stepping away from · what the break is for · what would signal readiness to reassess. Remains context-activated, separate from the primary sequence. |
| 4 | Correction | Design Spec · Product boundary | Added: the Relationship Playbook™ is a DISTINCT product (may share auth/infra/design/data), NOT part of the Companion; not a journal, course, chatbot, or coaching. Boundary map: Snapshot=recognition · Playbook=behavioral application · Companion=ongoing navigation · Academy=education. |
| 5 | Approval | UX Architecture v1 | Approved: 16-screen experience · recognition-gated branching · PRESENT → OBSERVE → DECIDE as the persistent behavioral spine · the OBSERVE two-lane timeline as the signature interaction · condition-based DECIDE logic · Intentional Dating Break context-activated · "My Relationship Play" completion artifact · Recognize → Practice → Use progress model · proposed MVP/v1.1 boundaries. |
| 6 | Ruling (definition) | Pattern-level reciprocity (UX §D.1) | The OBSERVE two-lane timeline represents reciprocity at the PATTERN level (mutual participation), NOT as mirrored turns, strict alternation, equal quantities, or 50/50 matching. The other person's contribution may take a different form (a response/follow-through can answer an initiation). Multiple non-identical contribution forms are made visible (initiation · response · follow-through · availability · consistency · curiosity/engagement · planning/contribution · word–action congruence). No numerical scorecard. Governing principle: reciprocity is information, not accounting. |
| 7 | Ruling | DECIDE neutrality + "gather more information" (UX §S10) | All outcomes remain co-equal (continue/deepen/slow/clarify/gather-more-information/discontinue); none is visually privileged. "Gather more information" must distinguish genuine insufficiency (reads unclear/not-yet-observable) from reluctance to act on available information (reads already present/absent), reflected neutrally — without telling the user what to decide. Prevents an indefinite escape hatch. |
| 8 | Ruling | MVP boundary | Signature OBSERVE interaction is IN the MVP (not deferred) — central to why the digital Playbook exceeds static content. Do NOT add: AI coaching · journaling · partner monitoring · streaks · points · badges · behavioral scores · relationship-health scores · additional primary Plays. |
| 9 | Status | Next phase | Content architecture + consumer copy for the approved UX, THEN implementation only after content + interaction states are approved. |
| 10 | Approval + Correction | Content Architecture & Consumer Copy v1 | Approved directionally with four corrections: S3 removed unsupported prevalence ("a lot of people do"); S4R softened fit-certainty ("gives more accurate information to work with", not "makes fit easy to see"); S7 reframed deficit → neutral ("What do you want to pay more attention to?" / "Interest can show up in different ways"), keeping "I already watch for these"; S13 removed the reset-vs-avoidance binary → intentionality-only ("what you want the break to do for you"), no classification. |
| 11 | QA / Status | Final copy QA (10-point) | PASS on all 10 (prevalence · mechanism · over-certainty · deficit assumption · covert advice · outcome-privilege · arbitrary timelines · scorekeeping/matched-effort · reading-level · therapy/diagnostic framing). Content Architecture & Consumer Copy marked **APPROVED FOR IMPLEMENTATION — CLUSTER 1 PROTOTYPE**. |
| 12 | Status | Next step | Implementation of the Difficulty Feeling Chosen prototype against the already-approved UX + content specs. Not generalized to other clusters; no additional Playbooks yet. |
| 13 | Implementation | Difficulty Feeling Chosen prototype built | Branch `playbook-dfc-prototype` (not production). 16-screen state machine + signature two-lane Reciprocity Timeline. Save bug fixed (Saved ✓ feedback + persists displayed play). Draft preview deployed; full E2E screenshots generated. |
| 14 | **Architecture pivot (v2)** | **Play Board replaces the linear 21-screen model** | The linear PRESENT→OBSERVE→DECIDE sequence is **retired as the product shape** (preserved as source). New shape: Experience Cluster → **Candidate Behavioral Pathways** → **Play Board** (self-recognition) → **Activated Play** (Recognize→Understand→Practice→Run→Save) → **My Plays**. Authored in `cluster-1-playbook-architecture-v2.md` (parts A–J). Board = **4 evidence-traced cards** (3 primary + 1 context-activated); no manufactured cards. Every v1 interaction re-homed (Reciprocity Timeline→Over-Pursuit; DECIDE Builder→Open-Ended Waiting; PRESENT recognition→Self-Editing; Dating Break→context card). **NOT implemented; not final copy; Cluster 1 only. Awaiting review.** |
| 15 | Methodology (definition) | **Candidate Behavioral Pathway vs Activated Play** | **Candidate Behavioral Pathway** = a behavior admissible enough (Tier 1 / governed Tier 2 / approved Tier 3) to *offer for self-recognition* — means "relevant enough to ask," NOT "people with this Cluster do this." **Activated Play** = a Candidate the user *confirms applies* and chooses to work with (first-party behavioral confirmation). |
| 16 | Ruling | Cross-Cluster Behavioral Evidence Rule — unchanged | The v2 Candidate→Activated distinction adds a **downstream, first-party recognition gate** that **operationalizes** the rule's non-prevalence guarantee; it does **not** modify any admissibility criterion. Rule NOT altered (would require separate approval). Status remains "Provisionally Validated — Cluster 1 Prototype." |
| 17 | Approval + Owner Decisions | Experience Design Pack — remaining four Plays v1 (`cluster-1-experience-design-pack-remaining-four-v1.md`) | **APPROVED** with 7 owner decisions: (1) `FidelityOutcome` becomes a **signature-tagged discriminated union**, each signature owning approved fields valued `demonstrated \| not_demonstrated \| not_applicable`, **no generic score**; (2) add `dualAttention`, `investmentView`, `communicationRehearsal`, `decisionRoom` to `InteractionKind` **now**; (3) *How Much to Put In* keeps "give a little less" as a legitimate option, **never** framed as withdrawal/game-playing/provoking pursuit — **no partner reaction shown as caused by the user's investment**; (4) *Say the Real Thing* v1 = **select-a-phrasing** (no free compose), **three** labeled hypothetical reactions, limited to **low-risk** preferences/opinions/small requests; (5) *Rest, or Giving Up?* persists **only** the bounded `chosen_stance` enum (`rest \| not_now \| lightly_open \| return_later \| reacting`); return-condition text ephemeral; ordinary fatigue/loneliness/discouragement **in scope**; support escalation only via already-approved structured-persistence/pervasiveness or crisis criteria; (6) *Is This Right for You?* mirror = **attention distribution + neutral fact recap**, no weighting/fit-score/good-bad; **author explicit exclusions** for mistreatment/unsafe rather than asking the user to "weigh" them; (7) **no new node kinds yet** — use existing primitives unless an authored graph shows a genuine unmet need. Two phrase tightenings applied: investment "driven by anxiety/hope" → **observable** "investment increasing without corresponding new relational evidence"; "that's the discouragement talking" → **"that's a forever conclusion showing up in a discouraged moment."** Authoring order set: (1) Is This Right for You? → (2) Rest, or Giving Up? → (3) How Much to Put In → (4) Say the Real Thing. One graph at a time; **design/copy only, no code/migration/deploy.** |
| 18 | Status | Next step | Author the full production-copy content graph for **Is This Right for You?** (`dualAttention`) only, for review. No code, no migration, no deploy; other three graphs not yet authored. |
| 19 | Approval + 4 Revisions | Experience content graph — **Is This Right for You? / `dualAttention`** (`cluster-1-experience-graph-is-this-right-for-you-v1.md`) | **APPROVED as the production Experience content specification**, after four applied revisions: (1) the exclusion beat reframed as a **respect/treatment boundary** — one mild dismissive moment is *not* classified as established mistreatment, treatment is *not* claimed unrelated to fit; teaches that ordinary fit differences and respect/safety information shouldn't be handled identically, one event ≠ character, a repeated pattern is more consequential, and coercion/intimidation/abuse stays excluded/safety-routed; (2) the mirror reports **observed exercise choices** (*"Where your choices focused in this exercise"* + one of three authored summaries) instead of pseudo-quantified attention (`a lot/some/a little` removed); neutral fact recap preserved; no score/percentage/weighting/trait; (3) `fit_information_used` → **`fit_information_kept_in_view`** (establishes fit-bearing info remained in the evaluative frame, **not** that it was used in a real decision); `evaluator_stance_held` unchanged; (4) tightened future-intent copy ("what I'd be signing up for" → "information I'd want to keep in view if long-term direction matters to me"; "a pretty straight answer about what they're looking for" → "real information about how they're talking about the future right now"). |
| 20 | Status | Next step | Author the full production-copy content graph for **Rest, or Giving Up?** (`decisionRoom`) only. No code, migration, wiring, or deploy; the other two graphs (How Much to Put In, Say the Real Thing) remain unauthored. |
| 21 | Approval + 10 Revisions | Experience content graph — **Rest, or Giving Up? / `decisionRoom`** (`cluster-1-experience-graph-rest-or-giving-up-v1.md`) | **APPROVED as production Experience content**, after 10 revisions: (1) stance `reacting` → **`pause_decision`** (never labels the user "reacting"); (2) fidelity `deliberate_decision_made` → **`intentional_stance_selected`** (intentionally postponing satisfies the operation without falsely claiming an engagement/rest decision); (3) **adaptive** reconsideration — bounded reads (rest/not-now) not challenged as merely emotional, the stronger temporal intervention primarily for the forever-conclusion read (`rgu-r1` reached only by option f); (4) removed "often a wise one"; (5) removed invented "somewhere better to put your energy" — reason **user-owned**; (6) "full stop, not a question mark" → **"a complete choice for now"**; (7) "chasing" → **"without actively pursuing right now"**; (8) **removed** the free-text return-conditions capture (re-entry conditions belong to the Play — eliminates a sensitive-text surface); (9) **persist the fidelity pair** (`intentional_stance_selected`, `discouragement_distinguished_from_conclusion`) alongside `chosen_stance`, per the discriminated `FidelityOutcome` architecture; (10) explicit: selecting the ordinary dating-specific "never going to happen" read does **not** independently trigger a support signpost — escalation governed only by the already-approved persistence/pervasiveness + Layer-A crisis rules. |
| 22 | Status | Next step | Author the full production-copy content graph for **How Much to Put In** (`investmentView`) only. No code, migration, wiring, or deploy; **Say the Real Thing** remains unauthored. |
| 23 | Ruling (fidelity signal name) | **How Much to Put In / `investmentView`** — second fidelity signal | Signal name **`effort_without_new_evidence_noticed`** is **approved**; do **not** revert to `compensatory_effort_recognized`. Rationale: the signal must describe the **observable relationship between effort and available evidence** (effort rose while no corresponding new relational evidence appeared), **not** infer *why* the user increased effort. `compensatory_effort_recognized` implies a causal/functional interpretation the Experience does not establish. Applied to the graph and the design pack (Play 4). This resolves the only flagged open item on the `investmentView` graph. |
| 24 | Approval | Experience content graph — **How Much to Put In / `investmentView`** (`cluster-1-experience-graph-how-much-to-put-in-v1.md`) | **APPROVED as production Experience content.** Three-round scenario (mutual stretch → quiet lull → independent reach-out); "give a little less" legitimate with the approved space-creation guardrail firing on every use; excluded vocabulary (stop-texting/mirroring/making-chase/withholding/scorekeeping/tit-for-tat) never modeled; the other person's beats fully independent of the user's investment (round-3 reach-out explicitly not caused by the user); observable target ("effort up without new evidence"); mirror recaps observed choices, no score; fidelity `investment_evidence_tied` + `effort_without_new_evidence_noticed` persisted per the discriminated `FidelityOutcome` architecture; no free-text surface. Remaining open items are provisional ids only (finalize at build). |
| 25 | Status | Next step | Author the full production-copy content graph for **Say the Real Thing** (`communicationRehearsal`) only — the fourth and final Cluster 1 Experience. No code, migration, wiring, or deploy. |
| 26 | Approval + signal-name confirmation | Experience content graph — **Say the Real Thing / `communicationRehearsal`** (`cluster-1-experience-graph-say-the-real-thing-v1.md`) | **APPROVED as production Experience content.** Three low-risk moments (preference / opinion / small request); **select-a-phrasing** (no free compose, no free-text surface); **three fixed, clearly-labeled hypothetical reactions** per moment, shown in full on every path (decoupled from the user's choice); success is never "did they like it"; R1 low-risk only, higher-risk material routes to approved supported/excluded routes. **Fidelity signal names CONFIRMED:** `preference_expressed_clearly` + `unnecessary_self_erasure_avoided` (observable, choice-based; refine the pack's `expressed_clearly` / `self_erasure_recognized`, applying the #23 observable-signal principle). |
| 27 | Milestone | **All four remaining Cluster 1 Experience content graphs authored + approved** | The six-signature Cluster 1 Experience set is complete at the content/design level: `conclusionNarrowing` + `evidenceTimeline` (live, unchanged) and the four newly-approved graphs — `dualAttention` (Is This Right for You?), `decisionRoom` (Rest, or Giving Up?), `investmentView` (How Much to Put In), `communicationRehearsal` (Say the Real Thing). **Implementation is a SEPARATE, later phase requiring its own approval:** add the four signatures to `InteractionKind`; build the signature-tagged discriminated `FidelityOutcome` union; transcribe content; author the proposed JIT literature entries; finalize provisional Play/sim ids; migration + wiring. No code, migration, wiring, or deploy performed in the design phase. |
| 28 | Approval (implementation roadmap) | Cluster 1 Experiences — implementation roadmap (plan file) | **APPROVED.** Three owner decisions set the shape: (a) **design the 4 Plays first** (an Experience can't be reached in-app without a schema-valid Play at its `playId`); (b) **extend the `reveal` node** (not a new node kind) to render the approved computed reveals (mirror / summary-select / choice-recap / labeled reactions), which the static `reveal` cannot — this is the "genuine unmet need" decision #7 reserved; (c) **in-session persistence for v1** — defer migration 0053 + sanitizer/progress wiring (Phase D). Phases: A design 4 Plays → B shared infra (signature-tagged `FidelityOutcome` union + per-signature fidelity aggregation + extended `reveal` + `InteractionKind` additions + signature-aware `events.ts`/`validateSimulation`) → C per-Experience build one vertical slice at a time (start `dualAttention`) → D deferred persistence. All behind `PLAYBOOK_REV3_ENABLED`; two live Experiences + v0 unchanged; nothing deployed. |
| 29 | Deliverable (Phase A) | Play Design Pack — remaining four Plays (`cluster-1-play-design-pack-remaining-four-v1.md`) | Authored for owner review. Defines each of the four Plays (`is-this-right-for-you`, `rest-or-giving-up`, `how-much-to-put-in`, `say-the-real-thing`) as the real-life tool its Experience rehearses: recognition gate, shift, core steps mapped to existing screen kinds (**no new screen kinds**), the frozen `myPlaysTemplate` five-field executable output, Play-level `fidelity` (correct/misuse/notMeaning), portable reminders, realWorldUse + safety note, and how the Experience's in-session fidelity signals inform the Play. Play ids finalize the Experiences' provisional `teach.toPlayId`. Four open Play-layer decisions flagged (literature depth; Play-4 re-entry-condition persistence; outputEditor scope; scenario content). Awaiting direction before authoring full per-Play specs one at a time. |
| 30 | Approval (direction) + rulings | Play Design Pack — remaining four Plays | **APPROVED in direction** with rulings, incorporated into the pack. **Cross-Play:** (1) L1 required every Play, **L2 optional** (only when directly necessary for correct use — don't duplicate the Understand/Field Guide layer); (2) Play-4 re-entry conditions live in the **Play**, may persist in the saved output but **optional** (never required); (3) `outputEditor`: Is This Right for You? **none**, Rest/How Much/Say the Real Thing **each get one** (Say the Real Thing saves/edits the bounded real-world sentence); (4) author **new compact micro-practice** items per Play, not the Experience scenarios verbatim. **Play-specific:** Is This Right for You? shift reworded observationally ("it can become easy to spend more attention on whether they want you than on what you're learning about whether you want this"); Rest, or Giving Up? reframed as **choosing an intentional revisitable stance toward dating right now** (not necessarily an engagement/rest decision — `pause_decision` co-equal), fidelity → "You chose an intentional stance for now and kept it revisitable"; How Much to Put In gate → "I sometimes change how much I'm putting in before I actually have new information," **`ruleBuilder` is core** (not optional) and governs the user's own evidence-and-decision process (never mirroring/waiting/withdrawal/response-time-matching/pursuit-test), and **a repeated observable pattern of non-engagement can itself be evidence** (distinguish filling an information gap vs responding to an observable pattern); Say the Real Thing gets an `outputEditor` for the bounded sentence and "say it kindly, once" → "Say the real thing clearly and kindly, without burying it in apology or explanation," strictly R1. **Fidelity naming:** one canonical field name per signal across Experience → `FidelityOutcome` → Play → persistence → Change Path, **no aliases.** |
| 31 | Deliverable (Phase A) | Full Play production spec — **Is This Right for You?** (`cluster-1-play-spec-is-this-right-for-you-v1.md`) | Authored for owner review — screen-by-screen exact copy (shift · literature L1+L2 · learn · new compact `scenarioSort` micro-practice · ownTurn · sentenceBuilder · output · portable · realWorldUse), output construction, frozen `myPlaysTemplate`, fidelity copy + observable conditions, portable, real-world-use handoff, and the canonical `evaluator_stance_held`/`fit_information_kept_in_view` Experience→Play handoff. No `outputEditor` (ruling #3). No code/migration/wiring/deploy. Other three specs not yet authored. |
| 32 | Approval | Full Play production spec — **Is This Right for You?** (`is-this-right-for-you`) | **APPROVED as production Play content.** |
| 33 | Deliverable (Phase A) | Full Play production spec — **Rest, or Giving Up?** (`cluster-1-play-spec-rest-or-giving-up-v1.md`) | Authored for owner review. Screen-by-screen (shift · literature L1 only · learn · new compact `scenarioSort` sorting thoughts into *feeling now* / *forever conclusion* / *stance I could choose* · `emotionBeat` honoring the feeling without pathology · ownTurn · output · portable · realWorldUse) + `outputEditor` (edit stance + optional re-entry). Operation = choose an intentional revisitable stance for now (`pause_decision` co-equal); fidelity "You chose an intentional stance for now and kept it revisitable." **Schema constraint surfaced:** Play `Screen` kinds have **no bounded single-select**, so the 5-way stance is captured via an `ownTurn` text field seeded with the five canonical stances as `suggestions` (soft-select) — no new screen kind. Re-entry conditions **optional** (never required). Support-signpost boundary in `safetyNote`; the ordinary "never going to happen" read is not a signpost trigger. Canonical handoff signals `intentional_stance_selected`/`discouragement_distinguished_from_conclusion`/`chosen_stance`. No code/migration/wiring/deploy. |
| 34 | Approval | Full Play production spec — **Rest, or Giving Up?** (`rest-or-giving-up`) | **APPROVED as production Play content.** |
| 35 | Deliverable (Phase A) | Full Play production spec — **How Much to Put In** (`cluster-1-play-spec-how-much-to-put-in-v1.md`) | Authored for owner review. Screen-by-screen (shift · literature L1 + L2 [the space guardrail, verbatim, as directly-necessary correct-use content] · learn · new compact `scenarioSort` sorting into *observable evidence (incl. a repeated pattern)* vs *a gap I'm filling / a feeling* · ownTurn · **`ruleBuilder` as CORE executable output** · output · portable · realWorldUse) + `outputEditor` (edit evidence + rule, like RD). Gate = "I sometimes change how much I'm putting in before I actually have new information." The if-then rule **governs the user's own evidence-and-decision process** with a control-check that forbids mirroring / waiting tactics / withdrawal / response-time matching / pursuit tests; **a repeated observable pattern of non-engagement counts as evidence** (distinguish filling an information gap vs responding to an observable pattern). Excluded vocabulary never modeled. Same bounded-single-select schema constraint → investment choice seeded via `ownTurn` suggestions. Canonical handoff signals `investment_evidence_tied`/`effort_without_new_evidence_noticed`. No code/migration/wiring/deploy. |
| 36 | Approval | Full Play production spec — **How Much to Put In** (`how-much-to-put-in`) | **APPROVED as production Play content.** |
| 37 | Deliverable (Phase A) | Full Play production spec — **Say the Real Thing** (`cluster-1-play-spec-say-the-real-thing-v1.md`) | Authored for owner review — the **fourth and final** Play spec. Screen-by-screen (shift · literature L1 only · learn [says-it → clarity → response-is-information, not a grade] · new compact `scenarioSort` sorting phrasings into *says the real thing* / *buries it in apology* / *smooths it over* · ownTurn · sentenceBuilder · output · portable · realWorldUse) + **`outputEditor` that saves/edits the bounded real-world sentence** (the actual preference/opinion/small request they intend to communicate). "say it kindly, once" → "Say the real thing clearly and kindly, without burying it in apology or explanation." **Strictly R1/low-risk**; higher-risk routes to approved supported/excluded. Canonical handoff signals `preference_expressed_clearly`/`unnecessary_self_erasure_avoided`. No code/migration/wiring/deploy. **With this, all four Play specs are authored** (Phase A design deliverables complete pending approval). |
| 38 | Approval + Milestone | Full Play production spec — **Say the Real Thing** (`say-the-real-thing`); **Phase A complete** | **APPROVED as production Play content.** All four Play production specs approved (Is This Right for You? · Rest, or Giving Up? · How Much to Put In · Say the Real Thing). **Phase A (design the four Plays) is COMPLETE.** Proceeding to **Phase B — shared infrastructure** (first code): signature-tagged discriminated `FidelityOutcome` union + per-signature fidelity aggregation + extended `reveal` node + `dualAttention`/`decisionRoom` added to `InteractionKind` + signature-aware `events.ts`/`validateSimulation`; behind `PLAYBOOK_REV3_ENABLED`; two live Experiences + v0 unchanged; no migration/deploy. |
| 39 | Code (Phase B1) | Signature-tagged `FidelityOutcome` union landed (foundational refactor) | **DONE — tsc clean, 327/327 tests pass, `npm run build` green.** `FidelityOutcome` is now a **signature-tagged discriminated union** (`lib/playbook/contentSchema.ts`); the per-option reconsider fragment split out as `ReconsiderFidelity` (now optional on `ReconsiderOption`); added `ChosenStance` enum; `dualAttention`+`decisionRoom` added to `InteractionKind`. Engine (`lib/playbook/simulation.ts`): `aggregateFidelity` dispatches by signature and **tags** the two live signatures' output (behavior-preserving), `notApplicableFor(signature)` replaces the flat constant, `validateSimulation` validates reconsider fidelity only when present. `events.ts` `simulation_completed` payload is signature-aware (bumped schemaVersion 2→3). Fixtures/tests updated to carry the signature tag. **The four new signatures currently return the not-applicable outcome** (their per-signature aggregators + the extended `reveal` renderer are **Phase B2**, to be validated against the first Phase C vertical slice). Two live Experiences + v0 behavior unchanged; nothing deployed; no migration. |
| 40 | Code (Phase B2 + first Phase C slice) | **`dualAttention` implemented end-to-end** (Is This Right for You?) | **DONE — tsc clean, 336/336 tests pass (+9 new), `npm run build` green, console-clean in preview.** **B2 machinery** (reusable): per-option semantic **`signal`** tags on `SimOption`/`ReconsiderOption` (fidelity computed from authored tags, not hardcoded ids); the **extended `reveal` node** (`computedSummary` [code resolver + authored variants] · `recap` · `reactions`) + `resolveRevealContent` + the `REVEAL_RESOLVERS` registry, rendered by `SimulationPlayer`; the **`dualAttention` fidelity aggregator** (`evaluator_stance_held`, `fit_information_kept_in_view`); `SIGNATURE_TITLE`/experience-library blurb. **Slice content** (`content/playbook/is-this-right-for-you.ts`): the `is-this-right-for-you` **Play** (from the approved spec), the `sim-itr-evaluator-stance` **simulation** (from the approved graph, incl. the computed two-column mirror), and 3 **JIT** entries (`lit-jit-wanted-vs-compatible`/`-respect-vs-fit`/`-liking-vs-choosing`); wired into `content.plays`/`.simulations`/`.literature`. Verified in preview: the Experience library now lists the third scenario, it steps into the sim (signature chrome + JIT hooks + signal-tagged decisions), and the computed reveal path is exercised. Behind `PLAYBOOK_REV3_ENABLED`; two live Experiences + v0 unchanged; nothing deployed; no migration. **Remaining:** `decisionRoom`/`investmentView`/`communicationRehearsal` slices (their aggregators + Plays + sims + JIT), then Phase D (durable persistence, gated). |
| 41 | Code (Phase C slice) | **`decisionRoom` implemented end-to-end** (Rest, or Giving Up?) | **DONE — tsc clean, 345/345 tests pass (+9 new), `npm run build` green, console-clean in preview.** Engine: `decisionRoom` aggregator (`intentional_stance_selected` incl. `pause_decision` as a legitimate stance; `discouragement_distinguished_from_conclusion` — non-punitive; `chosen_stance` from `stance:<enum>` signal tags) + the `decisionRoomStance` reveal resolver; `SIGNATURE_TITLE`/blurb. Content (`content/playbook/rest-or-giving-up.ts`): the `rest-or-giving-up` **Play** (from the approved spec, incl. `outputEditor` + the seeded stance field), the `sim-rgu-decision-room` **simulation** (no other person; the adaptive reconsider = forever-conclusion path only; the computed stance-summary reveal), and 3 **JIT** entries (`lit-jit-fatigue-not-verdict`/`-loneliness-signal`/`-rest-not-giving-up`); wired into content. Verified in preview: the Experience library lists the 4th scenario, stepping in renders the sim (chrome + JIT + the 6-way state sort + the 5 co-equal stances), and the reveal shows the correct computed stance summary ("Rest — you're setting dating down for a while."). Behind the flag; two live Experiences + v0 unchanged; nothing deployed; no migration. **Remaining:** `investmentView` + `communicationRehearsal` slices, then Phase D (gated). |
| 42 | Code (Phase C slice) | **`investmentView` implemented end-to-end** (How Much to Put In) | **DONE — tsc clean, 353/353 tests pass (+8 new), `npm run build` green, walked end-to-end console-clean in preview.** Engine: `investmentView` aggregator (`investment_evidence_tied`, `effort_without_new_evidence_noticed`) using **round-context signal tags** (`increase_at_lull` / `claimed_evidence_at_lull` / `named_no_evidence`) so the flat signal set captures the per-round evidence relationship; the reveal uses the **`recap`** field (no resolver needed). `SIGNATURE_TITLE`/blurb. Content (`content/playbook/how-much-to-put-in.ts`): the `how-much-to-put-in` **Play** (from the approved spec, incl. **`ruleBuilder` as core output** with the control-check forbidding mirroring/waiting/withdrawal/response-time-matching/pursuit-tests + the `outputEditor`), the `sim-hmp-investment-view` **simulation** (3 rounds — mutual → lull → **independent** reach-out; teaching in the investment options' inline feedback; the mirror **recaps the reader's own investment choices per round**), and 3 **JIT** entries (`lit-jit-mutual-signal`/`-effort-outruns-evidence`/`-relational-space`); wired into content. Verified in preview: library lists the 5th scenario; walked all 3 rounds (the effort-without-evidence teaching fires on the lull "more"; the independence note fires; **the recap reveal shows the three investment choices next to what was there**). Excluded vocabulary never modeled. Behind the flag; two live Experiences + v0 unchanged; nothing deployed; no migration. **Remaining:** `communicationRehearsal` slice, then Phase D (gated). |
| 43 | Code (Phase C slice) | **`communicationRehearsal` implemented end-to-end** (Say the Real Thing) | **DONE — tsc clean, 360/360 tests pass (+8 new), `npm run build` green, spread verified console-clean in preview.** Engine: `communicationRehearsal` aggregator (`preference_expressed_clearly` = clear-or-buried majority ≥2/3; `unnecessary_self_erasure_avoided` = clear majority) via a new **`selectedSignalCounts`** multiset helper + the `communicationRehearsalRecap` resolver. Content (`content/playbook/say-the-real-thing.ts`): the `say-the-real-thing` **Play** (from the approved spec, incl. the bounded-sentence `outputEditor`, strictly R1), the `sim-stt-rehearsal` **simulation** (3 low-risk moments; **select-a-phrasing** with teaching in each option's feedback; a **fixed 3-labeled-reaction spread** per moment — the `reactions` reveal field — shown on every path so the reaction is decoupled from the choice; a closing recap), and 3 **JIT** entries; wired into content. Verified in preview: the library lists the 6th scenario; the `reactions` spread renders (It lands easily / They'd prefer otherwise / They meet you partway), decoupled from the reader's phrasing. Behind the flag; two live Experiences + v0 unchanged; nothing deployed; no migration. |
| 44 | **Milestone** | **All four remaining Cluster 1 Experiences + Plays implemented (Phases B + C complete)** | The full **six-signature** Cluster 1 set now runs end-to-end behind `PLAYBOOK_REV3_ENABLED`: `evidenceTimeline` + `conclusionNarrowing` (original) and the four new slices — `dualAttention`, `decisionRoom`, `investmentView`, `communicationRehearsal` — each an Experience (sim) + Play + JIT, wired into content. Signature-tagged `FidelityOutcome` union; per-signature aggregators from authored `signal` tags; the extended `reveal` node (all three fields — `computedSummary`, `recap`, `reactions` — now exercised + browser-verified). **360/360 tests green; `npm run build` green; v0 + the two live Experiences unchanged; nothing deployed; no migration.** All six recognition pathways now map to built Plays (stale "coming soon" comments/tests updated). **Remaining: Phase D only** — durable persistence (run migration `0053` + extend `sanitize.ts`/`progress.ts`), which stays **deferred and owner-gated**. |
| 45 | Code (Phase D) | **Durable persistence for the Rev 3 separated state** (owner confirmed migration `0053` ran) | **DONE (code) — tsc clean, 364/364 tests pass (+4 new), `npm run build` green.** `lib/playbook/sanitize.ts` now **carries + sanitizes** the five Rev 3 state columns (previously stripped): `simulation_state` (incl. the signature-tagged `fidelity` + `chosen_stance`), `practice_state`, `use_review_state`, `change_path_state`, `literature_state` — each bounded (size caps), enum-validated (fidelity states → `not_applicable`, stance → `pause_decision`, mission/report/performed enums), unknown/surveillance keys dropped, no free-text bloat; absent/invalid state stays absent. `lib/playbook/progress.ts` `loadProgress` selects + maps the columns (empty `'{}'` default → `undefined`), `saveProgress` upserts them (`?? {}` for the NOT-NULL jsonb). `PlaybookProgressRow` extended. Stale "awaits 0053" comments updated. **Scope note:** the **live authenticated DB round-trip was NOT verified by me** — the progress API is auth-gated and writes the real Supabase, which I can't drive without credentials; verification is unit tests + tsc + build. Owner should confirm end-to-end by signing in and checking that Experience state (sim completion/fidelity) survives a reload. **Safe to deploy now that `0053` has run** (the code reads/writes the new columns); **nothing deployed by me.** With this, **Phases A–D are complete** — the full six-signature Cluster 1 Experience+Play set is implemented, and its Rev 3 state persists across sessions once deployed. |
| 46 | Verification (E2E) | **End-to-end test of the assembled Rev 3 Cluster 1 flow** | **PASS (non-authenticated paths) — 364/364 tests, tsc clean.** Code gate: full suite + `npx tsc --noEmit` green. **Live DB precondition confirmed:** a read-only `select` of all five migration-`0053` columns (`literature_state`/`simulation_state`/`practice_state`/`use_review_state`/`change_path_state`) on the real `playbook_progress` **succeeds** (columns exist; a real row already present) — 0053 is fully applied. **Browser walkthrough** via `/playbook-preview` (dev server, `PLAYBOOK_REV3_ENABLED`), **console-clean throughout:** (1) Home three-bucket IA (Learn / Work on it / Keep & explore) + the "A useful next step" card on top; (2) *See It Play Out* library lists **all 6** scenarios; (3) **dualAttention** full walk — JIT dialog surfaced, both fit decisions + reconsider, computed reveal resolved to the **"held both"** variant, neutral mirror recap, teach → Play loads; (4) **decisionRoom** — 6-way read + 5 co-equal stances, computed reveal resolved to **"Rest — you're setting dating down for a while."**; (5) **investmentView** — 3 rounds (mutual→lull→independent), the **`recap`** reveal mapped all three investment choices to their round-context labels + the not-caused-by-you note; (6) **communicationRehearsal** — 3 moments, each **`reactions`** spread rendered (3 fixed labeled examples, decoupled from choice), final computed recap **"You mostly said the real thing — clearly, and without disappearing."**, teach → Play loads; (7) *Use a Tool* library lists **all 6** Plays with when-helps / what-it-helps / explored state; (8) *Explore Another Area* lists **all 6** recognition pathways, each mapping to a built Play (no "coming soon"); (9) back-nav verified (Explore→Home; sim-teach→"Back to board"). **NOT verified (auth-gated, no credentials):** the authenticated DB **save→reload round-trip** — owner must confirm by signing in that sim completion/fidelity survives a reload. Nothing deployed; production flag still off. |
| 47 | Change (owner-requested) | **Real-life uses now log MULTIPLE times over time** (was once-per-Play, then hidden) | **DONE — tsc clean, 368/368 tests (+4), verified in preview, console-clean.** Owner directive: "Let people log multiple real-life uses over time." Previously `recordUseReview` **overwrote** a single review per Play (`reviews[playId]: UseReviewSignals`) and the board's "I used this in real life" button **disappeared** once state hit `used`, so a second real-life use couldn't be logged. Now: (1) `UseReviewState.reviews` is a **chronological list per Play** — new type `UseReviewEntry = UseReviewSignals & { at? }` (ISO, display-only, bounded/untrusted); `recordUseReview(p, playId, signals, at?)` **appends** (never overwrites), capped at 50 entries, newest last; new `reviewEntries()` reader tolerates absent / legacy-single-object / list shapes and coerces. (2) Sanitizer accepts the list, coerces a legacy single object → one-element list, bounds entry count + `at`. (3) `changePath` reads the **latest** entry (semantics preserved). (4) Board card (rev3 only): the log button **stays available after `used`**, relabels to **"Log another real-life use"** once ≥1 exists, and a muted line shows **"Logged in real life · N× · last {date}"**. **v0 untouched** (the `used`-inclusion is `rev3 &&`-gated; v0's separate `usedReviewFor` dialog never wrote `use_review_state`). Preview walk: logged RD twice — button relabeled, count went 1× → 2×, each open was a fresh blank form (append, not edit). Behind `PLAYBOOK_REV3_ENABLED`; DB `use_review_state` column already present; nothing deployed. **Surfacing note:** the repeat-log entry point lives on the "Where you might start" board card only; if the owner wants it on Home/My Plays too, that's a follow-up. |
| 48 | Change (owner-requested) | **Real-life-use logging also surfaced on Home + My Plays** | **DONE — tsc clean, 370/370 tests (+2), verified in preview, console-clean.** Owner: "Also surface it on Home and in My Plays." (a) **Home** (`ChangePathHome`, rev3-only): a persistent **"Log a Real-Life Use"** row in the *Work on it* bucket, targeting the current-focus Play (else the first explored Play that has a Use Review); desc shows the running count once logged ("You've logged '…' N× — add another…") or an invite at 0; routes via `onSurfaced({kind:"review"})`; suppressed when the same Play is already prompting via the pending "Look at How It Went" or the primary next-step review (avoids double review CTAs). (b) **My Plays** (`ExperienceShell`, `rev3 &&`-gated so v0 is untouched): each saved-Play card gets the **"Log another real-life use" / "I used this in real life"** button + **"Logged in real life · N× · last {date}"**, returning to My Plays (`backTo="myplays"`) on submit. Both reuse the existing append path (`recordUseReview` → list). **Content gap flagged:** only `read-and-decide` + `what-it-actually-means` have Use Review content, so structured repeat-logging accumulates for those two; the four new Cluster-1 Plays have **no Use Review authored**, so their "I used this" only marks-used (no count) — the Home row and count intentionally appear only where a Use Review exists. Preview: logged from Home (→ count 1×, returned Home) and from My Plays (→ count 2×, returned to My Plays). Copy is candidate; behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
| 49 | Content (authored, FOR REVIEW) | **Use Reviews authored for the four new Cluster-1 Plays** (closes the #48 gap) | **DONE — tsc clean, 370/370 tests, verified in preview, console-clean.** Owner: "Author the four missing Use Reviews." Added four `UseReview` objects to `content/playbook/finding-love-that-feels-mutual-usereviews.ts` (`MBR_USE_REVIEWS`, already spread into `useReviews` — no wiring change): `review-is-this-right-for-you`, `review-rest-or-giving-up`, `review-how-much-to-put-in`, `review-say-the-real-thing`. Each mirrors the two existing reviews' exact structure — `didDifferently` (multi, 4 operation behaviors + "Honestly, not much this time"), `performedOperation` (single, non-evaluative "how closely did you use the move: …" label + fixed yes/partly/no options), `becameClearer` (multi, 3 insights + "Nothing yet — still …"), `stuckWhere` (single, 3 friction points + "I didn't really get stuck") — and is **anchored to that Play's approved operation (positioning), its fidelity "correct" line, and its misuse boundaries** (e.g. HMP's stuck-options exclude tactics/scorekeeping; STT centers clarity-without-burying and response-as-information; RGU distinguishes discouragement from a forever conclusion and keeps rest non-failure; ITR keeps the respect/treatment vs fit distinction). **All six Cluster-1 Plays now have a Use Review**, so structured repeat-logging works across every tool on the board / Home / My Plays. Test updated (count 2→6 + per-Play coverage assertion). **Preview:** explored *Is This Right for You?* → "I used this in real life" now opens the **structured** review with its authored options (not the v0 mark-used dialog) → submit → marked used, "Logged in real life · 1×". **Copy is candidate, authored FOR OWNER REVIEW — not owner-approved.** Behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
| 50 | Change (owner-requested) | **Renamed to "Log a Real-Life Experience" + added an optional free-text description** | **DONE — tsc clean, 373/373 tests (+3 net), verified in preview, console-clean.** Owner: 'id call it "log a real life experience" and id also give them an option to fill in what the experience is.' (a) **Rename** across all rev3 surfaces — board card, My Plays, Home row — to **"Log a real-life experience" / "Log another real-life experience"** (Home title "Log a Real-Life Experience"); count line → "Logged N experience(s) in real life · last {date}"; Home desc → "You've logged N experience(s) with '…'". **v0 label unchanged** ("I used this in real life") — the rename is `rev3 ?`-gated. (b) **Optional free-text field** at the top of the Use Review — "What was the experience? (optional)" — stored per logged entry as `UseReviewEntry.experience` (bounded 2000 chars, trimmed, blank omitted), and **shown back as a quoted snippet** under the count on the board + My Plays cards. **Safety:** the note is **crisis-screened** via the same `onScreenText` → `/api/playbook/[key]/screen` (Layer A) path as the Plays' own-turn free text, screened on blur; it is the reader's own, and is **never emitted to the append-only event log** (metadata-only logging preserved). This is a deliberate, owner-requested exception to the Use Review's "bounded selects, no journaling" rule — `UseReviewSignals` stays purely functional (Change Path reads only the bounded signals; the free text never feeds it). **Preview:** logged an experience with a description → screen call fired (200) → note stored, count "Logged 1 experience", snippet quoted on My Plays. Copy is candidate; behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
| 51 | Change (owner-requested) | **History view — every logged real-life experience** | **DONE — tsc clean, 374/374 tests (+1), verified in preview, console-clean.** Owner: "yes create an option for history." Added a read-only `history` view to `ExperienceShell` (new `View` member + `historyFor` state + `openHistory()`), reached via a **"View all"** link appended to the count line on the board card and the My Plays card (rev3-only). It lists **every** logged experience for that Play, **newest first**, each as: the date (`fmtFullDate`, with year), the free-text note (quoted, when present), a plain-words fidelity tag (`PERFORMED_LABEL`: "Used it closely / some of it / Didn't really use it this time"), and — when set — "What I did" (didDifferently) and "Where I got stuck". Contextual back ("← Back to board" / "← Back to My Plays" per launch point). Kept **read-only** (logging stays on the cards/Home) to avoid return-nav ambiguity. **Preview:** logged two experiences on Read It, Then Decide → "View all" → history shows both newest-first with dates + notes + contextual back (per-entry fidelity/what-I-did lines covered by the new interaction test, which asserts the plain-words fidelity + newest-first order from set signals). Copy is candidate; behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
| 52 | Change (owner-requested) + bugfix | **"View all" on Home + Edit on My Plays cards (and a back-nav bugfix)** | **DONE — tsc clean, 376/376 tests (+3), verified in preview, console-clean.** Owner: "yes view all on the homepage as well.. also my plays can have an edit option." (a) **Home "View all →"** — added `"history"` to `SurfacedItem.kind` and routed it in `handleSurfaced` → `openHistory`; the Home "Log a Real-Life Experience" row now renders a "View all →" link beneath it when `logCount > 0`, opening that Play's history (`onSurfaced({kind:"history"})`). (b) **My Plays "Edit this Play"** — each saved-Play card (rev3-only) with an `outputEditor` gets an Edit action that opens the existing `OutputEditor` (`setEditingOutputFor`), prefilled, Save/Cancel; 5 of 6 Plays have an editor (not `is-this-right-for-you`). (c) **Bugfix found while testing:** My Plays launches its own sub-views (edit/log/history) which set the shared `backTo="myplays"` so they return to My Plays — but that corrupted My Plays' *own* Back (it pointed to itself). Added a dedicated `myplaysReturn` state, set at each My Plays open-point (home/board) and read only by My Plays' Back, decoupled from `backTo`. **Preview:** logged from Home → "View all →" appeared → opened history → Back returned Home; My Plays Edit opened the prefilled editor, Cancel closed it; **logged from My Plays then Back correctly returned Home** (was stuck-on-itself before the fix). Tests: Home "View all" routes `history`; My Plays Edit opens the editor + Back returns home. Copy candidate; behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
| 53 | Bugfix (owner-reported) | **Output editor made modal — no longer left stranded over another screen** | **DONE — tsc clean, 376/376 tests, verified in preview, server-compile clean.** Owner: "when I edit it and go back the edit screen still remains." Root cause: the `OutputEditor` rendered as a non-modal overlay (`{editingOutputFor && …outputEditor && …}`) ABOVE whatever `view` was active, so the underlying screen's Back was still clickable — navigating away (My Plays → Home) changed the view but left the editor pinned at the top. Fix: the editor is now **modal** — introduced `isEditing`, and the render is `{isEditing ? <OutputEditor/> : (<>…all views…</>)}`, so while editing the views are hidden and there is no navigation affordance except the editor's own Save/Cancel (both clear `editingOutputFor`). This makes the stranded-editor state structurally impossible, and improves the "Update it" (post-review) path too. **Preview:** opened Edit from My Plays → only the editor shows (no list beneath) → Cancel returns cleanly to My Plays. Test strengthened: while editing, the My Plays list is hidden; it returns on cancel. (Transient stale SWC syntax errors appeared in the browser console during the multi-step edit window — the current file compiles clean per tsc + server log + live render.) Behind `PLAYBOOK_REV3_ENABLED`; nothing deployed. |
