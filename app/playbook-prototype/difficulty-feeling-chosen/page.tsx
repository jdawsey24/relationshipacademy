import Experience from "./Experience";

// Difficulty Feeling Chosen — interactive Playbook PROTOTYPE (review build).
// Not the production paid Playbook; no entitlement gate yet (follow-up).
export const dynamic = "force-static";

export default function Page() {
  return <Experience />;
}
